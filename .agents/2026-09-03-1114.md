# Session: 2026-09-03-1114

**Authoring agent:** opencode
**Model:** muse-spark-1.2-contributor

## Objective
Consolidate optional-dependency UX around extras; `klea-mcp` was always installed but its `[mcp]` deps were not.

## Completed
- Added shared `klea_utils/imports.py:require_extra(str|list[str], extra)` — `find_spec`-only check with extra-level hint `pip install klea_utils[extra]`.
- Wired runtime guards (lazy, `--help` stays fast) into `klea-mcp` (`bs4`+`anydoc` → `[mcp]`), NiceGUI web client (`nicegui` → `[nicegui]`), and `klea-stores-create chunk/store/build` (`docling` → `[ingest]`).
- Annotated `console_scripts` with extras (`klea-mcp [mcp]`, `klea-stores-create [ingest]`) for metadata; noted `pip`/`uv` ignore it for conditional install — guard is the UX.

## Errors and Lessons
- Conditional `console_scripts` via `[extra]` is deprecated and ignored by `pip`/`uv` (installs wrapper anyway); `pip#9726` confirms — true gating requires runtime `require_extra` or a split distribution.

## Open Questions
- None

## Notes
- Keep heavy imports deferred per CLI conventions; `require_extra` is stdlib-only so cheap to call from command bodies.

## References
- packaging `entry_points` spec (extras on entry point deprecated)
- `pip#9726`: console_scripts extras ignored
