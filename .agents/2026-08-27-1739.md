# Session: 2026-08-27-1739

**Authoring agent:** opencode
**Model:** muse-spark-1.2-contributor

## Objective
Fix MCP `isError` reporting so tool execution failures correctly set `isError:true` per `mcp/types.py:1369` and `specification/2025-06-18/server/tools#Error Handling`; fix the `osb-data-repos.sqlite` "Not a sqlite database file" report.

## Completed
- Audited all `klea_utils/mcp/tool_impls/*.py` impls for consistent `error` field; fixed `list_files.py:65` `truncated` string → `bool`.
- Added `klea_utils/mcp/tool_result.py:to_result` helper mapping `error`/`Error` → `ToolResult(is_error=bool(error))` preserving `structured_content`.
- Updated wrappers to `ToolResult`: `klea_utils/mcp/server/bundled_tools.py` (`web_fetch`, `list_files`, `read_file`, `download_file`), `mcp_pkg/neuroml_mcp/tools/code_tools.py` (`list_files`, `run_python_code` `returncode`→`error`), `neuroml_mcp/tools/neuroml_tools.py` (`run_lems_simulation`, `get_models/repositories` normalizing `Error`), and `OSB/OSB-AI/mcp` (`sqlite_tools`, `biblio`, `repositories/*`, `download`, `osb_tools`).
- Removed transient client-side `error`-field normalization (`dispatch.py`/`tools.py` strict per ADR); confirmed `osb-repositories.sqlite` validity and env `OSB_DATA_DB_PATH` misconfig.
- Added `devdocs/adr/0003-mcp-iserror-compliance.md` (strict producer-only, ADR A chosen).
- Removed sqlite from Klea bundled exposure (impl stays framework-agnostic).

## Errors and Lessons
- Wrapping a `dict` without `ToolResult` always yielded `is_error=False` via `fastmcp/tools/base.py:359`; raising `ToolError` strips `structured_content` — `ToolResult` is the only path that preserves both.
- `list_files` string `truncated` was the only bool/string inconsistency across impls.

## Open Questions
- None

## Notes
- Verification: `utils_pkg` 71 + `mcp_pkg` 14 + `OSB/mcp` 17 tests pass; `ruff format`/`ty` clean; manual `Client(...).call_tool("query_osb_sqlite")` now `is_error:false` on success, `true` on `no such table`.
- OSB `store/osb-data/osb-repositories.sqlite` is valid SQLite 3 (6.9M); error was wrong `OSB_DATA_DB_PATH`/`cwd` (`mcp/store/` vs `store/`).

## References
- MCP spec `server/tools#Error Handling`, `mcp/types.py:CallToolResult`, `fastmcp/tools/base.py:convert_result`, `mcp/server/lowlevel/server.py:_make_error_result`
- ADR `devdocs/adr/0003-mcp-iserror-compliance.md`
