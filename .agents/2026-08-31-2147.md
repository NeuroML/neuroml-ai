# Session: 2026-08-31-2147

**Authoring agent:** opencode
**Model:** muse-spark-1.2-contributor

## Objective
Pre-release audit of `klea_rag` + `klea_utils` (code-only, then test coverage) and incremental fixes for P0/P1 weak points under the shard-pattern (small diffs, shared constants).

## Completed
- Audit: ~38 utils + ~40 rag findings; coverage gaps: 70% of P0/P1 `NOT COVERED` (6 missing rag test files, dispatch ordering, SSRF redirect, state mutations).
- P0 fixes: `dispatch.py:92` preallocated `results` + `gather(return_exceptions=True)` + `copy.deepcopy` ContextVar (`graph/base.py:44` `default=None` + `api/chat.py:57` `reset`); RAG alias bugs (`generate_retrieval_query.py:169`, `answer_from_context.py:87`, `retrieve_info.py:121`, `stores/utils.py:305` deep-copy); SSRF redirect manual `follow_redirects=False` + per-hop `check_ssrf_async` + `MAX_REDIRECTS=5` centralized in `ssrf.py:22`; `graph/base.py:660` `sys.exit`->`ValueError(400)` + `graph is None`->`RuntimeError(503)` + `/query` `is_ready` gate.
- P1 fixes: `llm.py:460` `int()` fallback, `parse_model_name` validation, `load_prompt` traversal; `route_evaluator.py:113` `and->or` + `schemas.py:24` `ge=0,le=1` clamp; `retrieve_info.py:138` per-retriever `try` + `inc_k` return check; `sessions_db.py:71` WAL/busy_timeout/foreign_keys + `app.py:64` lifespan `aclose`; `download_file.py:29` 100 MiB streaming + symlink re-check; `api/chat.py:25`/`sessions.py:28` `Field(pattern=r"^[^:]+$")` colon validation; `config.py:30` `Field(ge=1)` + `model_validator` `k_max>=default_k`; `stores/utils.py:84` `is_relative_to` parent fix; `api/utils.py:27` `HttpUrl` + `sse.py:53` `Timeout(300)` + json guard; `api/server.py:221` sync health poll + `start_new_session`/`killpg`; `mcp`: `list_files` `..` removed, `sqlite` URI `quote`, `download` streaming; `rag`: `init_rag` `query_domains` reset, `truncate` round-robin, `tool_results` per-tool `2500` via `textualize(max_len_per_tool)`, `graph/base.py:355` BM25 schema.

## Errors and Lessons
- `dispatch` `insert` ordering was *coincidentally* correct for sorted `A`; `preallocated` makes it obvious.
- `ContextVar(default={})` `B039` plus shallow `dict()` copy left nested `{"chat": {...}}` alias; needed `deepcopy`.
- `..` substring in `list_files` over-blocked `a..b`, under-blocked `ftp://`; `resolve().relative_to(root)` is the real gate.
- `store==source` + `chroma` heuristic over-excluded docs; fixed with `parent != source_resolved` guard.

## Open Questions
- `T3` coverage (6 missing rag tests)  ---  defer post-`0.2`.
- `init_rag` `retrieval_query` reset kept (one extra write, fail-safe) vs revert to `a598242` comment.
- `truncate` round-robin fairness vs per-domain `crossed`  ---  kept global `crossed` with round-robin.

## Notes
24 commits ahead of `origin/development`; shard pattern kept (no shared `stream->file` helper); verifiers in `/tmp/opencode` removed; `T0.4` pickle kept as trusted per-workspace.

## References
- Audit gaps: `devdocs/` not public; `docs/concepts/mcp.rst` for tool tags.
- Verifiers: `/tmp/opencode/t02_verifier.py`, `t03_verifier.py` (removed).
