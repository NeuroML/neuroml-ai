# Session: 2026-08-31-2147

**Authoring agent:** opencode
**Model:** muse-spark-1.2-contributor

## Objective
Pre-release audit of `klea_rag` + `klea_utils` (code-only, then test coverage) and incremental fixes for P0/P1 weak points under the shard-pattern (small diffs, shared constants).

## Completed
- Audit + coverage gap matrix; `T0.4` pickle closed as trusted per-workspace (shared drives must be trusted).
- P0: `dispatch` preallocated + `gather(return_exceptions)` + `ContextVar` `deepcopy`/`reset`; RAG alias deep-copy; SSRF `MAX_REDIRECTS=5` shard + `check_ssrf_async` `to_thread`; `graph` `400`/`503` gates + `Field(min_length=1)`.
- P1: `llm` `int` fallback + `parse_model_name` + `load_prompt` `relative_to`; `route_evaluator` `and->or` + `ge=0,le=1`; `retrieve_info` per-retriever `try` + `inc_k`; `sessions_db` WAL/`busy_timeout`; `download` 100 MiB stream; `chat` colon `Field(pattern)`; `config` `ge=1` + `k_max>=default_k`; `utils` `is_relative_to`; `validate_url` `HttpUrl`; `sse` `Timeout(300)`; `server` sync `httpx.Client` + `killpg`.
- `2.8` second half: `list_files` `..` removed -> `check_path_access`, `permission` empty-path, `sqlite` `quote`, `server` tests now mock `httpx.Client`/`monotonic`/`killpg` (16 passed), `truncate` round-robin kept and test updated to `A=1,B=1`.
- `2.9`: `init_rag` reset kept (one extra write) per your commit, `tool_results` `textualize(max_len_per_tool=2500)` (total cap removed per your note), `hard refs` reverted to evaluator-only.

## Errors and Lessons
- `dispatch insert` coincidentally correct; `preallocated` + `return_exceptions` obvious.
- `ContextVar` `deepcopy` needed for nested `{"chat":{"api_key":...}}`.
- `..` substring brittle; `resolve().relative_to` is gate.
- `httpx` lazy import in `server.py` broke `mock.patch("server.httpx.Client")`; moved to top-level `import httpx`.
- `store==source` `chroma` heuristic over-excluded; fixed with `parent != source`.

## Open Questions
- `T3` `init`/`evaluator` trivial routers  ---  cancelled per your note.
- `truncate` `1,1` vs `2,0`  ---  kept `1,1` round-robin as committed.

## Session Close 2026-08-31-2330
- Prompt-cache ADR `0028` `accepted` `2026-08-31`  ---  `file:line` stripped, `560k` metric removed, `load_prompt->output_schema->memory` reorder + `recent` forward `messages[max(0, summarised_till-2):]` via `get_last_n_conversations` (keeps last `N=2` for continuity), `num_history_chars` removed from `BaseLLMNode`, `cache_control` moved to `ainvoke` when `model_provider` known (Anthropic `ephemeral` only), `cached_tokens` logged via `usage_metadata`.
- `T3.3a/c` `RRF` dedupe `file+content` + `T3.3b` `BEST_EFFORT_WARNING` idempotent + `T3.2` `dispatch`/`validate_url`/`sse` `9` tests (`858+56` `not localonly` green).
- Next `kanban` `prompt-cache` issue deferred per your call.

## References
- Audit gaps: `devdocs/` not public; `docs/concepts/mcp.rst` for tool tags.
- Verifiers: `/tmp/opencode/t02_verifier.py`, `t03_verifier.py` (removed).
- ADR: `devdocs/adr/0028-prompt-cache.md` `accepted`.
