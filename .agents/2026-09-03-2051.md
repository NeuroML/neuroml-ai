# Session: 2026-09-03-2051

**Authoring agent:** opencode
**Model:** muse-spark-1.2-contributor

## Objective
Implement ADR-0030 general mode (mode as checkpoint truth, surfaced per node, live streaming to UI).

## Completed (committed)
- `KleaAgentState.mode` `general|scientific` default `general` (`schemas.py:82`), `GeneralConfig.mode` (`config.py:24`), `InitGraphState` preserve (`nodes/init_graph.py:53`), `mode` surfaced via `NodeStreamData` `details.mode` at every `agent` node (`goal_setter`, `planner`, `evaluator`, `answer_user`, `tools_router`, `init`), `klea --mode` flag (`agent/ui/cli.py:17` via `utils/klea_utils/ui/cli.py` `mode_default`), `devdocs/system/streaming.md` contract + `utils/klea_utils/ui/web/nicegui` inspection-tab fix.
- `agent/klea_agent/nodes/mode_check.py` `Checking mode` node wired `START→Initializing→Checking mode→Checking safety` (`klea_agent.py:205`) with `has_stores()` gate for scientific without source.
- `utils/klea_utils/interrupt/dialog.py` `InterruptDialog`/`Action` generic model, `klea_utils/api/interrupt.py` `POST /resume`, `klea_utils/graph/base.py` interrupt forwarding (`__interrupt__`/`interrupts` → `type:"interrupt"`), `klea_utils/ui/web/nicegui/runner.py` `handle_interrupt` generic dialog.

## Reverted / Not committed (utils-coupled)
- `utils/klea_utils/api/mode.py` `GET/PUT /mode`, `utils/klea_utils/api/chat.py:29` `ChatPayload.mode` + guard/`aupdate_state` `as_node="Initializing"` fallback, `utils/klea_utils/api/sessions_db.py:42` `mode` column, `utils/klea_utils/graph/base.py:889` `complete` `mode`, `utils/klea_utils/ui/web/nicegui/client.py` `fetch_mode`/`set_mode`, `state.py:70` `mode`, `runner.py:778` `_mode_badge` (`positive` green vs `grey-5`), `repl.py:59` `mode` label, `utils/klea_utils/ui/cli.py` `mode_default` plumbing, `agent/api/main.py` `create_mode_router` wiring — all reverted pending refactor to keep `utils` generic.
- `needs_downgrade` flag (`schemas.py:82`, `graph/base.py` `complete` `needs_downgrade`) replaced by `NodeInterrupt(InterruptDialog)`; old `runner.py` `needs_downgrade` watchers removed.

## Errors and Lessons
- `utils` completely generic vs `agent` owns `mode` boundary wasn’t drawn first — leaked `mode` into `graph/base.py`, `api/chat.py`, `sessions_db`, `runner` (even `base.py` has `mode`) and required revert; `utils` should only know `InterruptDialog`→`NodeInterrupt`→`type:"interrupt"` + `POST /resume` `resume: dict` generic patch, `agent` owns `mode` (`schemas`, `config`, `api/mode`, `AgentChatPayload`, `mode_check`, header pill).
- `aupdate_state` without `as_node` is “Ambiguous” on empty `thread_id` — need `as_node="Initializing"` fallback + `logger.debug` trace; `astream_events` `version="v3"` surfaces `NodeInterrupt` as `__interrupt__` (ainvoke) vs `interrupts` tuple (astream) — base must handle both and also check checkpoint via `aget_state` after the loop.
- `astream_events` token stream is wired but UI correctly ignores `type:"token"` and renders only on `complete` (markdown determinism).
- `runner.py` 1.5k lines monolith makes header/mode UI hard to keep generic — split into `components/` with `header_extra` hook is cleaner than duplicating `runner` per app.

## Open Questions
- `SessionStore` `mode` column vs generic `extra` JSON — keep column (fast header hydration) or keep `utils` column-free and read `mode` only via `aget_state`?
- `ChatPayload.mode` as first-query initializer vs `PUT /mode` only between turns — allow `general→scientific` on empty new `chat_id` (`store.get_chat is None` + no `__interrupt__` history) or strictly `409`?
- `runner.py` split now vs later — `header_extra` callback for `mode` pill or keep monolithic with `mode` guarded by feature flag?

## Notes
- Next rewrite order (agreed): `InterruptDialog` generic in `utils` (keep) → `graph` interrupt forwarding (keep) → `agent` `mode` API (`GET/PUT`, `AgentChatPayload`) + `SessionStore` mode cache → `agent` `ModeCheck` `has_stores()->InterruptDialog` → `agent` header `mode` component → `_pre_exec` stub (`mode=="general"` skip future `Verify`) → `graph interrupt helper` docs (`rag` init restart vs `agent` resume) → `ADR-0031` streaming + `ADR-0032` interrupts (queued after impl, `NNNN` MADR).

## References
- ADR-0030 agent operating modes, ADR-0029 correctness, ADR-0013 inspection, ADR-0019 shared nodes, `devdocs/system/streaming.md`, `utils/klea_utils/graph/base.py:779` `run_graph_astream_events`, `langgraph` `NodeInterrupt`/`Command`/`interrupts`
