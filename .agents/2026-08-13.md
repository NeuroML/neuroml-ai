# Session: 2026-08-13

**Authoring agent:** opencode
**Model:** deepseek-v4-flash

## Objective

Investigate whether increasing the RAG retrieval depth (k) actually surfaced
newly retrieved documents, and fix the two bugs found: a double k-increment
per `retrieve_more_info` cycle, and a hardcoded 10-document cap that discarded
lower-ranked chunks once k grew past it.

## Completed

- Confirmed the cap bug: `rrf_merge(..., num_refs_max=10)` truncated the fused
  results to 10 per domain regardless of k, and `reference_material` was
  replaced (not accumulated) each pass, so docs ranked 11+ were never used.
  Also confirmed a double increment: both `RouteEvaluator` and
  `RetrieveInfoNode` called `inc_k()` per cycle (k jumped 5 -> 7 -> 9 -> 10).
- **Single k-increment**: added non-mutating `can_inc_k()` to
  `BaseKleaRetriever`; the router now routes with `any(r.can_inc_k() ...)`
  (inlined after review) and never mutates k; `RetrieveInfoNode` is the sole
  `inc_k()` caller.
- **Size-based context budget**: replaced the fixed 10-doc cap with
  `GeneralConfig.max_refs_size` (default 20000 chars). `rrf_merge` count cap
  is now optional; new `truncate_reference_material()` applies a single global
  char budget across domains in RRF order, always keeping the first doc that
  crosses the budget (never empty context). k now controls fetch depth,
  `max_refs_size` controls what reaches the answer LLM.
- Docs (`create-and-use-rag.rst`, `huggingface.rst`) and `CHANGELOG.md`
  updated; docs build green.

## Errors and Lessons

- A test using three documents with identical `page_content` silently collapsed
  to one because `rrf_merge` dedupes by content -- use distinct content in
  retrieval tests.
- `any()` short-circuits: the router helper "asks every retriever" only when it
  mutates state; a pure check can short-circuit, so inline `any()` and drop the
  module-level helper.

## Open Questions

- None.

## Notes

- `max_refs_size` default (20000 chars) was sized from a real one-round query:
  10 docs = 8.7k content chars, ~10.7k serialized (headers/metadata included).
- `k_max` is intentionally kept: it caps per-pass store fetch cost and pushes
  the evaluator loop from `retrieve_more_info` to `modify_query` once a query's
  top candidates are exhausted. Its docs wording was corrected to say it is NOT
  the context limiter.
- Verification: rag_pkg 23 passed, utils_pkg 286 passed (non-LLM), `ty` no new
  diagnostics, `klea-rag --help` OK, docs `make html` succeeded.

---

# Session: 2026-08-13 (bundled tools refactor)

**Authoring agent:** opencode
**Model:** deepseek-v4-flash

## Objective

Move the bundled MCP tools (web_fetch, list_files) into `klea_utils` as
framework-agnostic implementations that any app on `BaseLangGraph` can wrap,
replacing the agent-specific code with dead `klea_code` imports.

## Completed

- `klea_utils`: plain tool impls in `mcp/tools/`, shared registry
  (`tool_meta`/`register_tools`) and aiohttp-session lifespan; new `[mcp]`
  extra (aiohttp, beautifulsoup4).
- `neuroml_mcp` and `klea_agent` now consume the shared registry; the agent
  wraps impls via `klea_agent/tools/wrappers.py`.
- Tool discovery uses `@tool_meta` as the sole marker (function name is the
  tool name; undecorated helpers ignored, logged at debug). Dropped the
  `_tool` suffix everywhere: mcp_pkg tools renamed (e.g.
  `get_models_from_neuromldb_tool` -> `get_models_from_neuromldb`); this
  also fixed a latent bug -- `run_lems_simulation` was decorated but never
  registered.
- Deleted `code_pkg/` (leftover from the package rename).
- Verification: all package tests pass (341), `ty` no new diagnostics,
  pre-commit clean, `python -m klea_agent.tools.bundled` handshake OK.

## Next

- Tool-impl review (start a new session): web_fetch download size cap,
  bs4 -> anydoc swap, unused `list_files` params
  (`max_depth`/`include_files`/`include_directories`), custom User-Agent.
  See TODO in `klea_utils/mcp/tools/web_fetch.py`.
