# Session: 2026-09-02-1629

**Authoring agent:** opencode
**Model:** muse-spark-1.2-contributor

## Objective
Draft correctness-centred agent ADR before LangGraph wiring; sync `klea_agent` with `BaseLangGraph`/`BaseLLMNode` and add operating-modes ADR.

## Completed
- Created `devdocs/adr/0029-agent-correctness-architecture.md` (11 invariants + design properties + implementation implications — supersedes `ADR-0025`, marked `superseded by ADR-0029`); corrected `devdocs/system/c4-container.md` forward refs; refined problem statement to "correctness is a required property / topology makes correctness assessable" and separated invariants from implementation
- Synced `KleaAgentState`/`InitGraphState` with `RAGState` pattern (`klea_agent.py`, `schemas.py`, `nodes/init_graph.py`)
- Added lifecycle parity (`get_allowed_msgpack_modules`, `_pre_graph`, `get_graph`, `_update_plan_step_status` any-error) and `ToolsPicker` `model_type=chat` (future `reasoning`)
- Updated `GoalSetter` (`memory=False`, static goal) and `Planner` to use `BaseLangGraph.tools_info` + inspector streams; clarified `ExplorePlanner` as exploration-planning stage (defer replacement)
- Fixed `ToolsRouter`/`Evaluator`/`AnswerUser` to shared-router + `NodeStreamData` contract; kept evaluator TODO
- Reconciled `StateGraph` wiring order with `rag.py`; verified `Guard`/`SummariseMemory` identical to RAG
- Created `devdocs/adr/0030-agent-operating-modes-and-assurance-levels.md` (Scientific vs General modes, explicit assurance state) and tweaked to template with one-way downgrade, CLI `--mode` flag, and inform-or-restart branch

## Errors and Lessons
- Early invariant numbering drifted after merging grounding levels — audit needed before accepting (ADR-0029)

## Open Questions
- RAG transport for Scientific mode (direct stores vs HTTP) still to decide; graph retrieval/verify/human-review deferred to next ADR-0029 phase
- ADR-0029 ready for final review; mark `accepted` when graph reflects invariants

## Notes
- Agent loop still WIP (`agent_pkg/klea_agent/klea_agent.py:146`); ADR intentionally high-level so wiring can evolve; all increments verified via `ruff check --fix`/`ruff format`, `ty`, `pytest -v -k "not localonly"` (9 passed)
- ASCII graph requested; base correctness invariants remain per ADR-0029

## References
- ADR-0029, ADR-0025, ADR-0030, c4-container.md, `agent_pkg/klea_agent/klea_agent.py:182`, `AGENTS.md`
