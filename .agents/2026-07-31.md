# Session: 2026-07-31

**Authoring agent:** opencode

## Objective

Refactor token usage tracking for branching LangGraphs and improve MCP tool presentation in the NiceGUI frontend.

## Completed

- Replaced cumulative token updates with a reducer-backed shared `TokenUsage` model for RAG and Code graph state.
- Added `add_token_usage` in `klea_utils.graph.reducers`; normalized LangGraph dictionary channel values with `TokenUsage.model_validate()`.
- Refactored LLM nodes to extract per-node token deltas without reading the existing state total.
- Added session-wide frontend token accumulation using numeric usage details.
- Added shared `ToolInfo` metadata under `klea_utils.mcp.schemas`, including standard MCP `title` support and documented fields.
- Added human-facing titles to MCP tools and replaced graph-level `tools_description` plumbing with domain-scoped `tools_info` while preserving LLM prompt descriptions and parameter schemas.
- Updated RAG tool selection status to render titles and readable Markdown argument lists; kept structured tool details in debug events.
- Added reducer and tool-picker regression tests.
- Resolved a NiceGUI startup failure by recreating the virtual environment; the old editable-install environment had a stale `site-packages/klea_utils/ui` namespace collision.
- Confirmed NiceGUI `app.storage.user` is server-side storage keyed by a signed browser session cookie, with Starlette's default 14-day cookie lifetime. Removing `.nicegui` removes the server-side user storage.

## Verification

- Reducer tests: 4 passed.
- RAG tool-picker tests: 3 passed.
- MCP metadata and code-tool tests: 2 passed.
- Focused Ruff and formatting checks passed, excluding existing repository warnings.
- MCP title discovery smoke test passed.
- Full `ty check` continues to report existing repository diagnostics.

## Open Questions

- None currently blocking runtime testing of the complete web flow.

## Notes

- Token usage events use numeric values in `details`; the frontend maintains the running session total.
- Tool status uses `display`; tool-call names, arguments, and reasoning remain in debug details.
- Usage totals are intentionally in-memory for the active frontend session and are not persisted or hydrated after reopening.
