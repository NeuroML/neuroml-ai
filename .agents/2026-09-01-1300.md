# Session: 2026-09-01-1300

**Authoring agent:** opencode
**Model:** muse-spark-1.2-contributor

## Objective
Fix stale NiceGUI session 500 on HF rebuild, correct `BaseLLMNode` schema/memory prompt order, plus earlier `JsonPlusSerializer` allowlist.

## Completed
- `JsonPlusSerializer` allowlist: `BaseLangGraph.get_allowed_msgpack_modules()` + wiring in `utils_pkg/klea_utils/graph/base.py`; RAG adds `EvaluateAnswerSchema`/`RetrievalQueryOutput`, Agent adds `CodeSchema`/`PlanSchema`/`StepSchema`/`GoalSchema`/`ArtefactSchema`/`Discovery`
- NiceGUI stale storage: `utils_pkg/klea_utils/ui/web/nicegui/runner.py` — `_ensure_user_storage()`/`_user_storage_or_none()`/`_safe_set_user()` warn+recreate `storage-user-*.json` on `AssertionError` (stale cookie after `.nicegui` wipe), guard `dark_mode` bind and `chat_id`/`user_id` persists; `main_page` now `await _ensure_user_storage()` before `user_id` bootstrap
- Prompt order: `utils_pkg/tests/test_node_base_schema.py` — renamed `test_system_prompt_puts_schema_before_memory`, flipped assertion to `schema < memory` (stable prefix `load_prompt` → `output_schema` → `context_summary` per ADR-0028)

## Errors and Lessons
- NiceGUI `app.storage.user` assert is normal after ephemeral `.nicegui` loss — must recreate via `core.app.storage._create_user_storage(session_id)` not manual `_users` hack
- Sync `setup_layout` cannot `await` recreation — fallback to warn + skip bind, next request succeeds

## Open Questions
- None

## Notes
- Commits: `63fb9b8 feat(langchain): mark models for serialization`, `a48897a`, `de4626d`

## References
- langgraph `serde/jsonplus.py` / `_msgpack.py` `SAFE_MSGPACK_TYPES` and `JsonPlusSerializer` allowlist
